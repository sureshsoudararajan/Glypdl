<Page
    x:Class="Glypdl.Windows.Views.HistoryPage"
    x:Name="HistoryPageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Glypdl.Windows.Models"
    xmlns:vm="using:Glypdl.Windows.ViewModels"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel Spacing="20" MaxWidth="900" HorizontalAlignment="Stretch">
            <!-- Header & Search & Clear -->
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Download History" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold" VerticalAlignment="Center" />

                <AutoSuggestBox
                    Grid.Column="1"
                    PlaceholderText="Search history..."
                    Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay}"
                    QuerySubmitted="AutoSuggestBox_QuerySubmitted"
                    VerticalAlignment="Center" />

                <Button
                    Grid.Column="2"
                    Content="Clear All"
                    Command="{x:Bind ViewModel.ClearAllHistoryCommand}"
                    VerticalAlignment="Center" />
            </Grid>

            <!-- History List -->
            <ListView
                ItemsSource="{x:Bind ViewModel.HistoryEntries, Mode=OneWay}"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:HistoryEntry">
                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="12"
                            Margin="0,0,0,8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Title, Meta & Date -->
                                <StackPanel Grid.Column="0" Spacing="4">
                                    <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{x:Bind Uploader}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                                    <TextBlock Text="{x:Bind FormattedDate}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" FontSize="11" />
                                </StackPanel>

                                <!-- Size & Status -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Spacing="2" Margin="0,0,16,0">
                                    <TextBlock Text="{x:Bind FormattedSize}" FontWeight="SemiBold" HorizontalAlignment="Right" />
                                    <TextBlock Text="{x:Bind Status}" Foreground="{ThemeResource SystemAccentColor}" FontSize="12" HorizontalAlignment="Right" />
                                </StackPanel>

                                <!-- Context Actions -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="6">
                                    <Button
                                        ToolTipService.ToolTip="Open Folder"
                                        Command="{Binding DataContext.OpenFolderCommand, ElementName=HistoryPageRoot}"
                                        CommandParameter="{x:Bind}">
                                        <FontIcon Glyph="&#xED25;" />
                                    </Button>
                                    <Button
                                        ToolTipService.ToolTip="Download Again"
                                        Command="{Binding DataContext.DownloadAgainCommand, ElementName=HistoryPageRoot}"
                                        CommandParameter="{x:Bind}">
                                        <FontIcon Glyph="&#xE72C;" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
